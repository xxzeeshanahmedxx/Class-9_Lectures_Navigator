---
import Layout from '../layouts/Layout.astro';
import SubjectCard from '../components/SubjectCard.astro';
import subjects from '../data/subjects.json';
const siteUrl = 'https://9lectures.pages.dev';
---

<Layout
  title="Class 9 Physics, Math, Chemistry, Computer, Urdu Lectures – Punjab Board"
  desc="Free class 9 lectures for Punjab Board 2025. Watch physics, math, chemistry, computer, and urdu video lectures online. Complete chapter-wise course."
  canonical={siteUrl}
>
  <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3 sm:gap-4">
    {subjects.map((s, i) => (
      <div class={`animate-fade-in-up animate-delay-${i + 1}`}>
        <SubjectCard id={s.id} title={s.title} emoji={s.emoji} accent={s.accent} />
      </div>
    ))}
  </div>

</Layout>
